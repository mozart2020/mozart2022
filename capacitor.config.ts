import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gives.mozart.app',
  appName: 'mozartTest',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
